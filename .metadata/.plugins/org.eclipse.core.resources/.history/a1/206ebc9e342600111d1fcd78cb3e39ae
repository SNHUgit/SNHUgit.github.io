package com.cs499.organizer;

import java.util.ArrayList;
import java.util.Random;
import com.cs499.organizer.model.Task;

public class IdGenerator {
	
	
	public static String getFreshID() {
		Random random = new Random();
		int idGenerator = random.nextInt(8);
		String id = Integer.toString(idGenerator); 
		return id;
	}
	
	// I was going to try and 
	
	// Public way to get new ID
	public String getNewTaskId(ArrayList<Task> tasks) {
		String id;
		boolean duplicate;
		do {
	        id = getFreshID();
	        duplicate = false;

	        for (Task task : tasks) {
	            if (task.getId().equals(id)) {
	                duplicate = true;
	                break;
	            }
	        }
	    } while (duplicate);
	return id;
	}
	

}
